# Defines the input fields for the UI form
parameters:
  - name: hostname
    label: "Target Hostname or IP"
    type: text
    required: true
  - name: username
    label: "Username"
    type: text
    required: true
  - name: password
    label: "Password"
    type: password
    required: true
