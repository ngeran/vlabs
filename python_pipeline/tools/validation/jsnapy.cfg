[DEFAULT]
# Paths used by JSNAPy (must exist and be writable)
snapshot_path = /tmp/jsnapy/snapshots
test_file_path = /tmp/jsnapy/tests
tmp_path = /tmp/jsnapy/tmp

# Logging (file path must be writable)
log_file = /tmp/jsnapy/jsnapy.log
log_level = INFO